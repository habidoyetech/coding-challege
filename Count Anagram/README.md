```An anagram of a string is another string with thesame characters in the same frequency in any order. For example 'abc', 'bca', 'acb', 'bac', 'cba', 'cab' are all anagrqams of the string 'abc'. Given two arrays of strings, for every string in one list, determine how many anagrams of it are in the other list. Write a function that receives dictionary and query, two strings arrays. it should return an array of integers where each element i contains the number of anagrams of query[i] that exist in dictionary.```